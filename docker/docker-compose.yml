version: "3.1"

services:

  db:
    image: postgres:14.2-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: <>2qp9DrZ[xV)OWZ
      POSTGRES_USER: salus
      POSTGRES_DB: salus
    ports:
      - "5432:5432"